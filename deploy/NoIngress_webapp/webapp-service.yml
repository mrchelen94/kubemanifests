apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort # Or ClusterIP if only internal access
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
      nodePort: 30090
